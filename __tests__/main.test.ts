import { glob } from '../src/main.js';

describe('Sample Test', () => {

    beforeAll(async () => {

    });

    beforeEach(async () => {

    });

    afterAll(() => {

    });


    it('should return Hallo', () => {
        expect(glob).toBeDefined();
    });

});
